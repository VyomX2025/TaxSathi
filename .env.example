# Required for /api/tax-agent (server only — never use NEXT_PUBLIC_ prefix).
# Vercel: Project → Settings → Environment Variables → add GEMINI_API_KEY for Production/Preview/Development as needed.
GEMINI_API_KEY=
